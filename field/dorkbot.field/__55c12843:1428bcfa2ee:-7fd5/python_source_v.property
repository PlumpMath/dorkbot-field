string>R = 0.41818187
G = 0.30545458
B = 0.26909083

def draw():
	f = FLine(filled=1, fillColor=Vector4(R, G, B, 0.2))
	f.circle(50 * _t, 200, 400)

	_self.lines.clear()
	_self.lines.add(f)

_r = draw

draw()
